"""ICE 产业链浏览器（PRD Part 06 统一对象模型）。

包结构：
    ice.models       — Pydantic v2 Schema（Object/Relation/Layout/Package）
    ice.repository   — Loader + Validator + Cache
    ice.services     — GraphService + SearchService
    ice.renderer     — Markdown 渲染
    ice.cli          — CLI 入口
"""
